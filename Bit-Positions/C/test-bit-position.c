#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include "bit-position.h"

/*
static void die(const char *message) {
    perror(message);
    exit(1);
}
*/

int main(int argc, char **argv) {
    return 0;
}
